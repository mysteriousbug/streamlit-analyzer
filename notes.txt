coming to the python screen lock on desktop. i want to make some modifications. i want to send a dummy file via email (preferable one with many pages), and once the recipient of the email downloads the file, i want the screenlock script to run in the background. the user should not have to do anything. after downloading the attachments and 10 seconds are up, the screen should automatically lock. it should only open with the password "1234", nothing else is allowed. no file encryption and all - just this.
